package com.gothicvanitympc.portfolioplanner;

import com.github.mikephil.charting.charts.PieChart;
import com.github.mikephil.charting.data.PieData;
import com.github.mikephil.charting.data.PieDataSet;
import com.github.mikephil.charting.data.PieEntry;
import com.github.mikephil.charting.utils.ColorTemplate;

import java.util.ArrayList;
import java.util.List;

public class pieChartCreate {
    //create piechart using MPAndroidChart


    void createPieCrt(float[] amt) {

/*
        String portfolioC[] = {"Large Cap", "Mid Cap", "Small Cap", "Developed Market", "Emerging Markets", "Specialty", "Real Estate/REITs", "Domestic Fixed Income", "International Fixed Income"}
        List<PieEntry> pieEntries = new ArrayList<>();

        for (int i = 0; i < amt.length; i++) {
            pieEntries.add(new PieEntry(amt[i], portfolioC[i]));
        }
        PieDataSet dataSet = new PieDataSet(pieEntries, "Investment Portfolio");
       dataSet.setColors(ColorTemplate.JOYFUL_COLORS);
        PieData data = new PieData(dataSet);

        PieChart chart = (PieChart) findViewById(R.id.idPieChart);
        chart.setData(data);
        chart.invalidate();

*/
    }
}